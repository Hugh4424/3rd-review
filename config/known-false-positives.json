{
  "description": "Confirmed false-positive bundle patterns to exclude from reviewer prompt (FR-BUN-001/002). Each rule: matchField (target|reason|sourceType), matchType (exact|substring|regex), pattern, enabled, addedTs, sourceWorkflowIssue (provenance), note. Default: empty/disabled = no filtering.",
  "rules": [
    {
      "id": "example-disabled",
      "matchField": "reason",
      "matchType": "substring",
      "pattern": "__EXAMPLE_NEVER_MATCHES__",
      "enabled": false,
      "addedTs": "2026-06-19",
      "sourceWorkflowIssue": "example — template only, disabled",
      "note": "Template showing the rule shape. enabled:false so it filters nothing."
    }
  ]
}
