## Review Design (post-term negation, no pre-negation cue)

The design persists reviewer_output via an attest record. Self-attest is
explicitly forbidden here, though every attest still requires an independent
verifier process.
