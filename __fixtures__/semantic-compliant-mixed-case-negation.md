## Review Design (case-insensitive negation, pre AND post, mixed case)

The design persists reviewer_output via an attest record. The process MUST NOT
self-attest under any circumstance.

Separately, Self-attest is explicitly Forbidden by policy, though every
attest still requires an independent verifier process.
